# One-shot scripts used to mutate index.html during editing sessions.
# They hardcode an absolute path, have already been applied, and are not
# part of the deck — but they sit next to it, so `git add` on this folder
# kept sweeping them in.
add_ai_evolution.js
fix_fonts.js
scale_fonts.js
update_presentation.js
upgrade_visuals.js
.vercel
